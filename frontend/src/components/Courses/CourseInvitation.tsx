import React, { useState } from "react";
import axiosInstance from "../../axiosConfig";

const CourseInvitation = ({}) => {
  //   const [inviteLink, setInviteLink] = useState("");
  //   const generateInviteLink = async () => {
  //     try {
  //       const response = await axiosInstance.post(`/api/courses/${courseId}/invite`);
  //       setInviteLink(response.data.inviteLink);
  //     } catch (error) {
  //       console.error("Error generating invite link:", error);
  //     }
  //   };
  //   return (
  //     <div>
  //       <button onClick={generateInviteLink}>Create Invite Link</button>
  //       {inviteLink && <p>Invite Link: {inviteLink}</p>}
  //     </div>
  //   );
};

export default CourseInvitation;
